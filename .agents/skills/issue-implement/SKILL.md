---
name: issue-implement
description: 指定された Issue の内容に沿ってコードを実装し、テストする。
---

# Issueを実装する

## 手順

1. Issue の内容を確認し、必要に応じて利用者に質問する。
2. Issue 用のブランチを作る。ブランチ名は `<種別>/issue-<Issue番号>-<内容を表す短い英語>` の形式にする（例: `fix/issue-1-user-id-ownership`）。種別は変更内容に応じて `fix`（不具合修正）、`feature`（機能追加）などを使い分ける。
4. 実装方針は、`AGENTS.md`または`CLAUDE.md`の作業ルールに従う。
5. 実装後は、利用者が実際に正常動作を確認できるよう、変更内容に対応した動作確認手順を提示する。

## 停止条件

- 仕様が曖昧で結果が変わる。
- 本番データや秘密情報に触れる必要がある。
- 破壊的な変更が必要になる。
- 想定より変更範囲が大きくなる。
- テスト、Lint、ビルドが失敗する。

このSkillではpushとPR作成を行わない。
